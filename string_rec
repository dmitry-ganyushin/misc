# recursevely capitalize a word

mystring = "mystring"

def capitalize_recursevely(astring):
  if len(string) == 1:
    return ""
  else:
    return astring[0].upper() + capitalize_recursevely(astring[1:])
 
capitalize_recursevely(mystring)